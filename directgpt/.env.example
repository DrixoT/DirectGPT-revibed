# DirectGPT does not need a server-side key: the OpenAI API key is entered in the
# browser (Settings) and stored in localStorage, then sent directly to api.openai.com.
#
# Nothing in this file is read by the app. It exists only to document that fact.
# OPENAI_API_KEY=sk-...   (not used)
